name: Test and build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - cartsense-android-build

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out CartSense
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.8
          channel: stable
          cache: true

      - name: Show tool versions
        run: flutter --version

      - name: Install dependencies
        run: flutter pub get

      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed lib test

      - name: Analyze source
        run: flutter analyze

      - name: Run tests
        run: flutter test

      - name: Build release APK
        run: flutter build apk --release

      - name: Prepare and validate installable APK
        shell: bash
        run: |
          cp build/app/outputs/flutter-apk/app-release.apk CartSense.apk
          test -s CartSense.apk
          sha256sum CartSense.apk | tee CartSense.apk.sha256

      - name: Upload CartSense APK
        uses: actions/upload-artifact@v4
        with:
          name: CartSense-Android
          path: |
            CartSense.apk
            CartSense.apk.sha256
          if-no-files-found: error
          retention-days: 30
